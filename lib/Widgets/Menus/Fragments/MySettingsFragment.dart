import 'package:flutter/material.dart';

class MySettingsFragment extends StatelessWidget {
  const MySettingsFragment({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.green,
    );
  }

}